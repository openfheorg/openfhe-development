# Published comparison targets

The three CSV files transcribe Tables 3–5 of the supplied
`Sparse_Hermite_Interpolation_Method_for_Discrete_CKKS_Functional_Bootstrapping.pdf`.
They are **published targets, not fresh measurements or raw benchmark logs**.
The current branch must be run to produce independently measured Tables 4–5.
BKSS denotes the optimized real evaluator (`BKSS_NEW`).
